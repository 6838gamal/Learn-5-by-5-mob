[build]
  command = "flutter build web --wasm"
  base    = "."
  publish = "build/web"

[build.environment]
  FLUTTER_VERSION = "3.24.5"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
