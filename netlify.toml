[build]
  command = "flutter build web"
  base    = "."
  publish = "build/web"

[build.environment]
  FLUTTER_VERSION = "3.22.0"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
